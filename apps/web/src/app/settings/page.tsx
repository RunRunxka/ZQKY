import { SettingsWorkspace } from '@/features/settings/SettingsWorkspace';

export const metadata = { title: '智启课源 · 设置' };

export default function SettingsPage() {
  return <SettingsWorkspace />;
}
