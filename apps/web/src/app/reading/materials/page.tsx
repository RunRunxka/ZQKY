import { MaterialLibrary } from '@/features/reading/MaterialLibrary';

export const metadata = { title: '智启课源 · 阅读材料库' };

export default function ReadingMaterialsPage() {
  return <MaterialLibrary />;
}
