# 部署规划

当前已有本机 Next.js 正式前端和 FastAPI 真实问答/模型后端；本目录仍只有部署规划，不含容器、公开环境或已部署服务。

未来确需部署时，再为 web 与 FastAPI 编写容器、持久卷和 HTTPS 反向代理，验证 SSE 流式响应、环境密钥、备份恢复和回滚。PostgreSQL、Redis、Worker 等只有在项目决定接入后才进入部署定义，不把历史规划写成既定依赖。

此目录未来保存部署定义，不保存真实密钥或用户文档。需要公开发布时先完成可检查的构建、配置和测试，再依用户授权执行最终发布。
