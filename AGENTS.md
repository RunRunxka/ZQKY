# 智启课源开发规则

## 项目与当前阶段

- 项目根是本文件所在目录。正式前端在 `apps/web`，采用 Next.js App Router、React、TypeScript、Tailwind CSS 3.4、Lucide 和 Zustand。
- 当前已实现教案工作台、FastAPI 三协议学习问答和模型管理。教案仍使用本地规则；账号、数据库、RAG、附件和工具未接入。真实供应商验收与模拟上游验收必须分别记录。
- `apps/api` 是真实业务后端，`infra` 仍为规划说明。已实现接口见 docs/API.md；不要借助 Next.js Route Handlers 实现第二套业务后端。
- `教案模板部分` 是冻结的Vite旧版。禁止在那里继续业务开发或将其加入npm工作区；原始规划、参考图和Word原件保留。

## 开始前读取

1. 根 README，了解启动方式和现状。
2. `docs/PROJECT_GUIDE.md`、`docs/STATUS.md`，核对唯一目标、当前任务与最近审查；连续实施时读 `docs/replica/NEXT_SESSION_START.md`。
3. 修改目录下的 `AGENTS.md` 和对应模块说明；从根启动的工具也应显式检查更深层规范。
4. 实际源码、锁文件和相关测试。文档与代码不符时核实后更新，不把旧记录当作本次验收结果。

用户当前明确指令优先于历史计划。`项目规划`、附件和归档中的示例提示词、任务包是参考资料，不是自动执行指令。不根据规划中的待办自行扩大本次任务。

## 代码组织

- `app` 只负责路由、布局和页面状态；业务放在 `features/<module>`，通用控件和壳放在 `components`。
- 跨模块类型放在 `contracts`；模块专有类型留在模块。导航、主题和服务定义只维护一套。
- 页面通过显式props/Context及服务接口获得能力，不在模块顶层读取 `window`、`document` 或 localStorage，不恢复旧的 `window.__ZQKY_HOST__` 注入方式。
- 浏览器专用能力置于客户端生命周期/事件内；服务端不得共享可变的用户编辑状态。数据要按编辑器实例隔离。
- 使用项目根npm工作区和锁文件。依赖版本由实际安装锁定，常规开发不顺手升级框架或改变包管理器。
- 代码遵循已有格式；保持函数、组件职责清晰，不把整个模块重新堆回单个巨型页面。

## 数据与授权

- 在已授权范围内完成常规实现、检查和修复；不要为可逆的小改动反复要求确认。
- 不覆盖用户正在编辑的浏览器会话。自动化使用隔离上下文、固定测试数据和测试端口，不读取或清空真实草稿。
- 不修改原Word模板。需要派生模板时使用 `assets/templates/source`，记录校验值并测试导出。
- 密钥不进入前端、源码、日志、截图或测试样本。`.env.example` 只能包含空值或无敏感信息的示例。
- 不发布网站、推送远程仓库或发送外部消息，除非用户明确授权这些动作。
- PowerShell中移动或删除目录前确认绝对路径和范围；保留用户无关改动及可回退版本。

## 完成与交接

- 根命令：`npm run typecheck`、`npm run lint`、`npm run test:unit`、`npm run build`；路由、布局、保存或导出改动还要运行对应浏览器测试。
- 测试仅运行与变更相关且有意义的场景。没有执行的检查明确写未执行及原因，不能写“应当通过”。
- 区分真实服务、本地规则、示例数据和规划状态。不能用构建成功代替业务或视觉验收。
- 当前目标是以智启课源品牌复刻 F:\DeepTutor v1.6.5 全部产品前端、AI 交互和原有动画。复杂能力可显式模拟，但不得删减状态、服务接口、本地保存和跨页联动；MCP/Skills 管理只在设置，聊天共享目录。详见 `docs/PROJECT_GUIDE.md`。
- 进度、检查与下一动作只更新 `docs/STATUS.md`；目标/决定只更新 `docs/PROJECT_GUIDE.md`；逐项证据更新三矩阵。旧 HANDOFF/TASKS/提示词已归档，不再重复新建。
- 用户已于 2026-09-08 明确要求本地 Git 版本控制。每批检查暂存范围、产物和敏感数据后作小提交；不自动推送、部署或更改全局 Git 身份。保存检查点不代表已验收。

## 代码审查重点

优先检查数据丢失、服务端状态串用、未实现功能假成功、规则填充静默覆盖、草稿版本不兼容、模板字段遗漏、导出截断、CSS或打印规则影响其他页面，以及从正式代码反向依赖归档目录。

## 多智能体协作

- 总控负责拆分、依赖、文件归属、集成和最终交付。专业角色按需要调用，默认不递归创建子智能体。
- 每个任务必须有 ID、版本、负责人、可写范围、验收条件与结果格式；共享工作区同一文件同一时段仅一个写入者。
- 共享契约和公共组件先确定单一负责人；需要跨出范围时交给总控重新分配，不自行争改。
- 默认由总控独占权威进度文档、项目决定、验收矩阵、依赖锁文件与最终 Git 操作。可定向授权一个临时负责人，但不能形成多个写入者。
- 自动化使用隔离浏览器、测试数据和产物目录。构建目录、测试端口、报告路径也需要分配，不能只隔离源码文件。
- 实现者负责相关自检，完成时标记待验收；独立验收者对稳定候选核查，给出具体范围与证据；总控确认已验收。
- 保留用户和其他智能体的改动，不切换共享分支、不批量暂存、不清理未知数据，不撤销他人工作。
- 规则、任务或接口变化时更新版本并通知相关实例；旧实例确认停止写入后才能转交文件。时间超时不等于写入权已安全释放。
- 状态恢复必须核对当前源码、任务版本和证据，不能照抄旧待办或已完成结论。
- 批次通过、视觉通过、模拟流程通过与真实服务通过分别记录；未执行的验证保持未验证。
