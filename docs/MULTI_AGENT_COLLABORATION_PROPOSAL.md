# 多智能体协作任务模板

更新：2026-09-13。本文提供可复用任务卡、结果卡、资源分配格式和外部团队启动提示词。实际规则以根 `AGENTS.md` 为准，当前任务需求、进度、候选 SHA、缺陷和下一动作只看 [STATUS](STATUS.md)。旧完整提案与演练可从 Git 提交 `6e3f642` 追溯，不作为当前项目指令。

## 角色

| 角色 | 责任 | 默认写权限 |
| --- | --- | --- |
| 总控 | 拆分任务、确定依赖和候选、分配资源、集成、更新权威文档、最终 Git | 公共契约、权威文档、矩阵、锁文件、Git |
| 取证 | 核对固定参考、源码、交互状态和验收参数 | 只读；证据写入专属产物目录时须由总控分配 |
| 实现 | 在有限文件范围完成一个行为闭环并做相关自检 | 任务卡列出的文件 |
| 验收 | 对稳定候选独立复现、检查失败路径和边界 | 默认只读；不得边验边修 |

本批按STATUS的MODEL-EXEC v3，外部队长就是临时实施总控，含代码、合同、文档及本地Git；本Codex只读审查。旧提示词/P0子卡中要求等待Codex冻结合同的规则已被用户新指令替代。

角色是职责，不要求固定平台或常驻实例。总控不得用智能体口头结论代替实际文件、命令结果和候选 SHA。

## 工作流

1. 总控先读取当前规则、STATUS、三矩阵和实际源码，确认用户本次授权。
2. 取证只回答有限问题：参考入口、状态机、数据边界、动画参数和可复现验收点。
3. 总控冻结契约和任务版本，再分配唯一文件写入者、测试端口、浏览器数据、构建目录和报告目录。
4. 实现者交付 `ready_for_review`，不得自行把模块标为已验收。
5. 总控固定待验候选；验收者记录 `pass / fail / not_run`，失败必须给触发条件与证据。
6. 总控决定 `pass / needs_revision / blocked`，更新 STATUS 和对应矩阵，显式暂存并提交。

## 任务卡

复制后填写，未分配的字段不得靠各智能体自行猜测。

```text
任务 ID：
版本：
负责人：
角色：

目标：
用户授权范围：
非目标：

依赖：
固定参考与提交：
开始候选 SHA：

可读范围：
可写文件：
禁止修改：

运行资源：
- 前端/API 端口：
- 浏览器数据目录或项目：
- 构建/测试目录：
- 报告与截图目录：

行为闭环：
1.
2.
3.

验收条件：
- 正常路径：
- 空态/错误：
- 取消/重试/恢复：
- 数据兼容：
- 学习问答视觉基准：
- 动画/减少动画：
- 真实服务与模拟边界：

必须运行：
结果格式：
超出范围时：
```

## 结果卡

```text
任务 ID / 版本：
负责人：
候选 SHA 或差异标识：
状态：ready_for_review | pass | fail | blocked

修改文件：
行为变化：
保留的数据与兼容方式：

验证：
- command：
  result：
  evidence：

首败与修复：
未执行：
真实服务边界：
视觉边界：
剩余风险：
需要总控决定：
```

## 写入与运行资源

- 同一文件同一时段只有一个写入者。公共组件、契约、权威文档、矩阵和锁文件默认由总控写。
- 共享工作区不切分支、不批量暂存、不清理未知文件、不回退其他人的修改。
- 测试端口、浏览器会话、测试数据、截图目录和 build 输出也必须有所有者；文件不冲突不代表运行资源不冲突。
- 验收期间候选保持不变。需要修复时结束该验收版本，升级任务版本后重新提交候选。
- 只通过 build、存在路由、存在组件或存在截图，均不足以将功能、视觉、动画或真实服务状态标为通过。

## 智启课源专用门槛

- 全站默认主页和视觉基准是当前学习问答 `/chat`，保留智启课源品牌和蓝色主题。
- 每个页面批次分别报告功能状态与学习问答视觉状态；业务工作区不强制套用聊天 236px 中栏或 912px 内容宽度。
- 主聊天生产只允许真实服务；测试替身留在测试目录。其他模块的显式模拟须完整保留加载、失败、取消、重试、恢复、保存和跨页状态。
- 真实供应商、隔离上游、本地规则、显式模拟和纯前端验收分别记录。
- 浏览器自动化使用隔离数据和 5174，不读取或清空用户在 5173 的草稿。

## 交接要求

交接只更新 [STATUS](STATUS.md)、对应三矩阵和必要契约文档。NEXT_SESSION_START 只保留接手方法，不复制计划；旧 TASKS、HANDOFF 和长提示词不再新建。提交前检查 `git diff --cached --check`、暂存范围、凭证和生成产物；不自动推送或部署。

## 可复制的外部团队提示词

把下面“队长启动提示词”交给你选用的其他 Agent 工具。角色文本用于工具的子任务/角色配置；若工具没有可靠委派、文件隔离和结果回传能力，由一个实现者串行执行，取证和验收保持只读。`AGENTS.md` 本身不会创建智能体或强制锁文件。任务的实时版本、详细需求和权限以 STATUS 第6节为准，本文不维护第二份计划。

### 队长启动提示词

```text
执行授权：MODEL-EXEC v3，替代你收到的旧启动提示词和P0子卡中的冲突权限。
工作目录H:\备份xuexi\智启课源。本轮从回退后的af9cb78产品代码继续，不恢复旧代码提交。
用户明确要求由你完成代码落地，本Codex只审查交付。
你是本批临时实施总控，拥有合同定稿、代码集成、文档更新、必要锁文件及本地Git提交权。

先读根AGENTS、README、docs/STATUS.md第6.1节、PROJECT_GUIDE的draft-2裁决、API、三矩阵、
apps/api/AGENTS.md和apps/web/AGENTS.md；核对HEAD与用户改动，保持工作分支不切换。
你的P0 v2已交付，不需要再次提交P0。确认旧子智能体已停止写入，升级任务卡后直接继续执行。
撤销旧“等待Codex冻结contract-v1”“只能P1A”“禁止修改schemas/前端契约/main/锁文件”的限制。

你直接负责并完成：
1. 按PROJECT_GUIDE的D1–D16修正，在队内定稿contract-v1并同步Python/TS/HTTP合同，记录文件散列。
2. 实现38条供应商、协议/适配器、发现、推理、专用认证、迁移、凭证一致性和错误处理。
3. 使用C:/Users/96022/.codex/skills/frontend-design/SKILL.md实现DeepTutor式模型管理UI，
   保留当前学习问答的蓝色主题、原功能与既有数据；完成前后端接线和聊天目录联动。
4. 完成相关工程/API/浏览器视觉与e2e测试、可用授权凭证下真实供应商测试，并修复发现的问题。
5. 更新现有STATUS/API/PROJECT_GUIDE/三矩阵，检查范围和敏感数据后作本地小提交，不推送/部署。

共享schema、前端contracts/services、model_views、main/lifespan及必要依赖均可由你团队直接修改。
每文件/运行资源指定唯一负责人，共享契约先定稿，再按依赖并行；必要变化队长自行协调，不交给Codex代写。
参考F:\DeepTutor固定42fab3cf429a1fbf36b257ab8d116a3814964202，只读、不升级。
模型范围内常规技术决定自行完成。若确有无法兼容的需求矛盾或需要用户实际登录，仅提出具体问题并继续独立工作。

H1：不cherry-pick/恢复d678ef3，不清除悬空对象或_work证据；按当前代码重新实现。
不要用回退后的权限判定d678ef3当时越权，其父cc32b8a已转交实施权；历史代码质量仍未确认。
H2：旧R-13探针只作待复验线索；固定模型/问题比较推理开关与预算，不能默认关闭推理掩盖失败。
finishReason=length仍需检查答案截断，旧候选结果不能算当前验收。
D11/D16：CodeBuddy的API Key与Copilot的GitHub令牌交换分开实现，不将Copilot当普通API Key。
不自动读第三方登录文件；受管认证缺真实登录条件时标明对应not_run/不可用，不假成功，不停掉整个开发。

统一管理8001、5174、.next-test、隔离浏览器和专属产物目录；不动8000/5173、正式.env/.local-data和真实草稿。
保留.zcode/agents两处用户改动及所有无关文件。测试密钥仅临时存储，不进入日志/源码/Git。
独立验收者只读稳定候选，失败交实现者修复复验。工程、视觉、模拟上游和真实结果分开记录。
必须持续推进至本批代码闭环；不要只回复确认、再提案、只完成注册表或只留接口空壳就结束。
最终返回代码提交号、变更文件、功能覆盖、实际测试证据、失败/未执行项、剩余风险与资源释放状态。
```

### 取证角色提示词

```text
你是只读取证者。只执行队长发给你的有版本任务卡，不自行扩大供应商/业务范围。
逐项核对固定DeepTutor的注册元数据、实际工厂路由、请求参数、认证与发现实现及相关测试。
输出：参考路径与行号、目标已有实现、缺口、可复用点、不能照搬的配置/凭证边界、建议验收场景。
发现“注册表声明”和“实际适配器”不一致时分别列证据，不凭名称猜支持。
不修改源码、权威MD、锁文件或Git；报告仅写队长分配的专属产物目录。不会访问真实密钥文件。
```

### 实现角色提示词

```text
你是有限范围实现者。开工前回报任务ID/版本、合同版本、候选及你独占的文件和运行资源。
只实现任务卡定义的行为闭环；共享契约变化先向队长提交最小建议，不能自行修改归其他人负责的文件。
沿用现有接口、错误信封、SSE、revision和凭证存储，保留旧ID及配置；无服务能力不能返回假成功。
用临时配置/内存凭证完成相关自检，保留首败并验证修复；真实服务与模拟上游证据分开。
完成时提交ready_for_review结果卡，不自行宣布已验收、不提交Git。
收到停止/交验后停止写源码与共享产物，回报未完成事项、服务进程及资源释放情况。
```

### 独立验收角色提示词

```text
你是独立验收者。先确认稳定候选HEAD+差异散列、任务版本、contract版本以及实现者已停止写入。
对任务卡每个条件输出pass/fail/not_run与命令、退出码和证据，不复述实现者结论代替验证。
重点复核跨连接凭证隔离、旧配置迁移、.env/JSON失败补偿、推理/正文拆分、SSE中途失败与取消、
模型发现来源、专用认证过期/取消，以及默认预算耗尽时是否假成功。
后端验收不宣称UI视觉通过；没有凭证的供应商真实调用写not_run；测试fixture通过不等于真实供应商通过。
只读源码，证据写专属目录，不边验边修、不改权威文档、不提交Git。
发现失败返回最小复现、影响和首败证据，由队长安排修复后固定新候选再验。
```
