# 多智能体协作任务模板

更新：2026-09-15。本文提供可复用任务卡、结果卡、资源分配格式和外部团队启动提示词。实际规则以根 `AGENTS.md` 为准，当前任务需求、进度、候选 SHA、缺陷和下一动作只看 [STATUS](STATUS.md)。旧完整提案与演练可从 Git 提交 `6e3f642` 追溯，不作为当前项目指令。

## 角色

| 角色 | 责任 | 默认写权限 |
| --- | --- | --- |
| 总控 | 拆分任务、确定依赖和候选、分配资源、集成、更新权威文档、最终 Git | 公共契约、权威文档、矩阵、锁文件、Git |
| 取证 | 核对固定参考、源码、交互状态和验收参数 | 只读；证据写入专属产物目录时须由总控分配 |
| 实现 | 在有限文件范围完成一个行为闭环并做相关自检 | 任务卡列出的文件 |
| 验收 | 对稳定候选独立复现、检查失败路径和边界 | 默认只读；不得边验边修 |

当前用户授权由外部队长担任实施总控，负责STATUS当前任务内的代码、必要契约、文档及本地Git；Codex审查，用户单独指定的文档整理除外。旧MODEL-EXEC/P0文本仅归档，不再决定新任务范围。

角色是职责，不要求固定平台或常驻实例。总控不得用智能体口头结论代替实际文件、命令结果和候选 SHA。

## 工作流

1. 总控先读取当前规则、STATUS、三矩阵和实际源码，确认用户本次授权。
2. 取证只回答有限问题：参考入口、状态机、数据边界、动画参数和可复现验收点。
3. 总控确定任务版本、必要共享接口及唯一文件/运行资源负责人；仅在有共享契约变化时队内定稿，不为普通任务新增外部签字或P0等待点。
4. 实现者交付 `ready_for_review`，不得自行把模块标为已验收。
5. 总控固定待验候选；验收者记录 `pass / fail / not_run`，失败必须给触发条件与证据。
6. 总控决定 `pass / needs_revision / blocked`，更新 STATUS 和对应矩阵，显式暂存并提交。

## 任务卡

复制后填写，未分配的字段不得靠各智能体自行猜测。

```text
任务 ID：
版本：
负责人：
角色：

目标：
用户授权范围：
非目标：

依赖：
固定参考与提交：
开始候选 SHA：

可读范围：
可写文件：
禁止修改：

运行资源：
- 前端/API 端口：
- 浏览器数据目录或项目：
- 构建/测试目录：
- 报告与截图目录：

行为闭环：
1.
2.
3.

验收条件：
- 正常路径：
- 空态/错误：
- 取消/重试/恢复：
- 数据兼容：
- 学习问答视觉基准：
- 动画/减少动画：
- 真实服务与模拟边界：

必须运行：
结果格式：
超出范围时：
```

## 结果卡

```text
任务 ID / 版本：
负责人：
候选 SHA 或差异标识：
状态：ready_for_review | pass | fail | blocked

修改文件：
行为变化：
保留的数据与兼容方式：

验证：
- command：
  result：
  evidence：

首败与修复：
未执行：
真实服务边界：
视觉边界：
剩余风险：
需要总控决定：
```

## 写入与运行资源

- 同一文件同一时段只有一个写入者。公共组件、契约、权威文档、矩阵和锁文件默认由总控写。
- 共享工作区不切分支、不批量暂存、不清理未知文件、不回退其他人的修改。
- 测试端口、浏览器会话、测试数据、截图目录和 build 输出也必须有所有者；文件不冲突不代表运行资源不冲突。
- 验收期间候选保持不变。需要修复时结束该验收版本，升级任务版本后重新提交候选。
- 只通过 build、存在路由、存在组件或存在截图，均不足以将功能、视觉、动画或真实服务状态标为通过。

## 智启课源专用门槛

- 全站默认主页和视觉基准是当前学习问答 `/chat`，保留智启课源品牌和蓝色主题。
- 每个页面批次分别报告功能状态与学习问答视觉状态；业务工作区不强制套用聊天 236px 中栏或 912px 内容宽度。
- 主聊天生产只允许真实服务；测试替身留在测试目录。其他模块的显式模拟须完整保留加载、失败、取消、重试、恢复、保存和跨页状态。
- 真实供应商、隔离上游、本地规则、显式模拟和纯前端验收分别记录。
- 浏览器自动化使用隔离数据和 5174，不读取或清空用户在 5173 的草稿。

## 交接要求

交接只更新 [STATUS](STATUS.md)、对应三矩阵和必要契约文档。NEXT_SESSION_START 只保留接手方法，不复制计划；旧 TASKS、HANDOFF 和长提示词不再新建。提交前检查 `git diff --cached --check`、暂存范围、凭证和生成产物；不自动推送或部署。

## 可复制的外部团队提示词

队长文本适合全新对话：提供目标与接手方法，**任务内容只从STATUS当前任务读取**，避免提示词与文档形成两套计划。平台不支持可靠并行时采用单实现者串行+只读验收；AGENTS.md不自动创建Agent或强制锁文件。

### 队长启动提示词

```text
你接手“智启课源”，担任实施总控。工作目录H:\备份xuexi\智启课源。
正式前端apps/web是Next.js/React/TypeScript，真实业务后端apps/api是FastAPI。
沿用根npm工作区与锁文件；“教案模板部分”是冻结旧版，不在其中开发，不新建第二套业务后端。

项目目标：以智启课源品牌复刻固定DeepTutor产品前端、AI交互与原有动画。
全站视觉以当前/chat学习问答为准，保留蓝色主题；模块功能/信息结构参考只读F:\DeepTutor，
固定SHA 42fab3cf429a1fbf36b257ab8d116a3814964202，不升级参考、不换品牌、不删原业务。
保留教案编辑/草稿/Word-PDF导出、真实问答/模型、所有本地数据和跨页来源关系。
主聊天仅真实服务，其他批准模块可显式模拟；模拟不得冒充真实，也不得省略状态与保存联动。
公共导航220/56px，手机模态抽屉；236px学习记录与912px对话宽度只属于聊天。

先核对git status --short、git log -5 --oneline，保留现场用户改动，不reset或切共享分支。
依次读根AGENTS/README、docs/PROJECT_GUIDE.md、docs/STATUS.md、三矩阵及修改目录更深规则。
NEXT_SESSION_START只提供接手方法；历史/archive及旧MODEL-EXEC/P0任务不是当前授权。
STATUS当前任务章节是本批唯一实施任务，先报告你核实的目标、任务ID、状态与文件/资源归属，然后直接实施。

由你完成代码、必要契约、集成、验收组织、权威文档与本地提交；Codex只审查交付，不替你写产品代码。
使用可用Agent工具分配有界工作：取证只读、实现者独占文件、独立验收者只读稳定候选。
每项任务列ID/版本/负责人/可写文件/依赖/验收/结果格式；默认不递归扩队。
公共样式、契约、文档、锁文件、端口、构建目录和Git均需单一负责人。
视觉任务必须使用frontend-design skill，实际查看/chat与参考，再产出可运行页面。
不要停在调研/设计/提案，不重做已完成模型批，不等Codex冻结普通接口。

只用隔离浏览器、数据和测试资源，不操作用户5173/8000、正式.env/.local-data或真实草稿。
只停止本批创建的进程，不读取其他AI工具登录文件，不输出密钥。
功能、视觉、动画、模拟上游、真实供应商分别给证据；缺某项凭证只阻塞该项实测。
独立验收发现失败后由实现者修复，再固定候选复验；不边验边改。
按实际变更运行适用工程和浏览器检查，不升级无关依赖，不修改断言掩盖失败。
Node26的已验证单测参数见README/STATUS，先核现有方法，不另开工具链升级任务。

更新STATUS及对应矩阵，长期决定写PROJECT_GUIDE，契约写API/ROUTES；不要新建重复HANDOFF/TASKS。
保留历史首败与未验范围；必要脱敏证据随Git保存，不能只依赖_work。
检查暂存范围、敏感数据与生成物后本地小提交，不推送、不部署、不改全局Git身份。
最终返回任务ID、起点/实现/最终SHA、改动、功能保留、各维度证据与pass/fail/not_run、剩余范围及资源释放。
批次通过不代表全站完成；完成当前已授权任务，不自行开展整份长期路线。
```

### 取证角色提示词

```text
你是只读取证者，只回答队长任务卡内的有限问题。
核查固定参考、现有源码、交互/数据边界及验收场景；给文件行号，区分事实、推断和未验证。
视觉不能只看组件名，真实能力不能只看类型/注册表。范围矛盾交队长，不自行开新任务。
不改产品、权威MD、锁文件或Git；证据只写队长分配目录，不读密钥，不抢占他人服务。
返回任务ID/版本、发现、复现建议与证据，然后停止写入。
```

### 实现角色提示词

```text
你是有界实现者，先核对任务ID/版本、起点、接口和独占文件/运行资源。
完成任务卡行为闭环，保持业务与数据兼容；共享变更交队长协调，不越界争改。
运行相关自检，保留首败；生产/模拟/测试替身分清，失败不假成功。
完成交ready_for_review，列差异、测试、未完成、进程与资源；不自行标整体验收或提交Git。
交验后停止写入，等待队长明确分配下一版本。
```

### 独立验收角色提示词

```text
你是独立验收者，先核任务版本、稳定候选SHA/差异、资源归属及实现者已停止写入。
按本批条件自建或复核有意义场景，不复述实现者结论代替验证。
视觉任务实际查看页面和截图；动画看过渡/结束/中断，不以CSS类名或截图存在判通过。
数据任务验证持久化及失败恢复；异步任务检查迟到/取消/重试；真实服务与fixture分别记录。
逐项输出pass/fail/not_run、命令/退出码/证据和准确范围。旧候选结果不得冒充本候选实测。
只读产品，证据写专属目录；发现失败给最小复现交队长修，不边验边改、不改权威MD或Git。
```
