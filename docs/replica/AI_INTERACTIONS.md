# AI 交互映射与验收边界

更新：2026-09-24（CHAT-CONTENT-MATH-AND-FOLLOW v1 更新 A-reasoning-math：正文流式 Markdown 接线、尾段公式与 reasoning follow 修复；候选待独立验收；真实供应商 not_run）。此前 UX-REGRESSION-FIX v1（推理流式期间公式即时渲染）与 UX-PERF-CLOSEOUT v1（推理流呈现与提交语义、模式菜单单层化与 RAG 模式入口、学习记录归属）历史报告不改写；**RAG 仍未接入**。主聊天仅真实SSE，R-03生产模拟残留已清；复杂能力/工具/附件真实执行仍未接。模型管理和供应商隔离合同已有局部独立验收；真实DeepSeek证据按模型/预算/推理开关分组，R-13未关闭，其他供应商不据此升级。阅读/写作/知识库/书籍的显式模拟单独标注。当前状态与候选索引见 [STATUS](../STATUS.md)，历史真实首败见 [2026-09-12结果](../qa/acceptance-20260912/summary.json)。

状态包含待实现、部分实现、实现待验收、已验收、实现待修复、真实服务未接入、已移除。下表“组件保留”不等于运行通道已接通；阅读/写作等模块的显式模拟与主聊天分开。

2026-09-22 main 审查补充：`A-books-pipeline` 保持部分实现，M22-01～03/05 为未修复生命周期/归属/并发/落库问题；3 个隔离探针复现幽灵执行器和旧修复写入新 run，历史 A1 不覆盖这些缺陷，见 [本轮证据](../qa/main-review-20260922/README.md)。**同日 H1-BOOKS-COMMIT-SAFETY v1** 把书籍集合写入改为互斥锁内事务并引入提交结果契约（已提交/冲突/记录不存在/不适用/读取失败/写入失败），重试耗尽不再返回候选值；创建/编辑/删除/笔记/作答/进度/生成事件全部同一协议，调用方只在落库成功后展示成功、失败保留输入——见 [CS 批次台账](../qa/H1-BOOKS-COMMIT-SAFETY/DEFECT-LEDGER.md)。仍为本地模拟执行器，不接真实 LLM/解析。**同日 H1-BOOKS-HARDEN v1 修复了这六项**（含 M22-04/06）：执行器统一收尾并区分删除/读失败/失权，修复操作身份冻结且迟到写入被拒，租约归属校验与失权即停，读取失败与最终落库失败都如实呈现并给恢复入口，编辑器方向键不翻页；原 3 个探针未修改、修复后按预期失败，正式回归断言正确行为，见 [批次台账](../qa/H1-BOOKS-HARDEN/DEFECT-LEDGER.md)。仍为本地模拟执行器，**不接真实 LLM/解析**；`A-ask-user` 与真实 RAG 仍未接入，条目不升级。

| id | 交互 | 原版来源 | 状态 | 现状与待办 |
| --- | --- | --- | --- | --- |
| A-send | 发起、取消、恢复 | features/chat/model/protocol.ts、transport | 部分实现 | 2026-09-12真实Flash发起、推理中停止（AbortError）、新会话2秒无晚到消息、真实错误后重试与刷新恢复通过；供应商远端计算/计费取消未观测，其他协议实际供应商未验。 |
| A-real | 真实流式问答 | transport | 部分实现 | 2026-09-13 B-MODEL-ACCEPT：普通非流式与SSE**分开**测量（`/test` stream:false 返回 application/json 单 JSON；`/chat/stream` 为 text/event-stream），纠正上轮用SSE冒充普通调用。DeepSeek两模型三档：默认2048零正文(EMPTY_RESPONSE)；关推理默认预算出正文但 finishReason=length（**截断，不算成功**）；受控8192 stop 完整结束。浏览器受控档首可见中文1172ms、KaTeX 12处0错误、停止/重试/刷新通过。R-13**仍未关闭**；Responses/Anthropic无可用凭证。 |
| A-reasoning-math | 真实推理展示/正文公式 | 固定 DeepTutor AssistantActivity、AnswerMarkdown/KaTeX | 部分实现 | Flash8192 与 2026-09-23 UX-REGRESSION-FIX v1 的**历史证据仅覆盖既有推理公式子项**：当时三协议 reasoning 受控流公式 27/68/107，未闭合尾段原文、补齐后转换；旧正文样例静态/终态可渲染，不证明流式 `message.content` 路径正确。**CHAT-CONTENT-MATH-AND-FOLLOW v1（2026-09-24，独立验收 pass，附 not_run 条件）**首败代码路径：助手正文流式仍只走 `AnswerMarkdown`，未接安全分段；合成 SSE 浏览器路径核对 `text.delta → store message.content → 正文 DOM`，源原文/增量拼接/IndexedDB 内容一致，覆盖 `$…$`、`$$…$$`、单反斜杠 `\(...\)`/`\[...\]`、列表/表格、`aligned`、代码围栏、双转义字面样本与未闭合尾段。修复后正文流中 8 个 `.katex`（2 display、0 error），尾段闭合后 11 个（0 error），KaTeX CSS/字体可见；终态与刷新 IndexedDB 原文不改。**独立验收用 KaTeX `annotation` 逐条证明四类定界符都是可见公式、MutationObserver 证明流式期间逐步可见，并核对 IndexedDB `content === 上游 bodyRaw`（345/345，firstDiffIndex=-1）**。双反斜杠 `\\(...\\)` 样本不是已确认真实供应商格式，仅作合成展示容错样例；真实供应商原始片段仍 `not_run`。同一批已复现 reasoning follow 首败（正文折叠后用户重开停在旧位置，gap=144px）；修复后独立验收实测：重开 10 次采样 gap 全 0，真实滚轮上滚后 top 恒 0 而 scrollHeight 275→423（约 2.2s/14 次采样）未被抢回，回底后 gap 全 0。性能：总控与验收各自复跑 20k/50k 均 rAF p95 5.7ms、>50ms 帧 0、长任务 0，验收另用 CDP 相位切分排除“每 delta 重解析整段正文”。证据 [CHAT-CONTENT-MATH-AND-FOLLOW v1](../qa/CHAT-CONTENT-MATH-AND-FOLLOW-20260924/README.md) 与 [FROZEN 记录](../qa/CHAT-CONTENT-MATH-AND-FOLLOW-20260924/FROZEN-CANDIDATE.json)。不升级真实供应商、用户人工视觉、完整异常终态或动画曲线通过范围。R-16 关闭口径见 STATUS §3。 |
| A-course-resource-safety | 课程资源目录可用性 | 目标自有（对照参考课程资源引用）；知识库/笔记本/书籍目录 | 已验收 | 2026-09-14 B-COURSE-RESOURCE-SAFETY：目录在 effect 中集中读取为容错快照，三态区分 available/missing/unknown；读取失败显示错误与重试、不断言目标已删除、不阻断课程正文/大纲/其他目录、不自动删引用；e2e `course-resource-faults.spec.ts` 9 项含故障→重试→修复→恢复与手机视口 |
| A-course-sessions | 课程 → 学习会话（创建/恢复/课程↔聊天往返） | 参考 `courses/[courseId]/page.tsx` 的课程会话列表与 `?course=` 交接；目标为既有 IndexedDB 会话库 + `Conversation.courseId` + 轮次课程快照 | 已验收（前端闭环；真实供应商 not_run） | 2026-09-22 H1-COURSE-SESSIONS v1：课程详情列出本课程会话（只按 `meta.courseId` 过滤）；新建会话保存成功后才跳转、同一 tick 连点只建一条、写入失败保留页面可重试；聊天页显示所属课程与「返回课程」；发送时把课程名/约定（≤1200 字符）/大纲摘要/资源**登记**清单冻结为 `TurnCourseSnapshot`，渲染成一条 `system` 消息插在请求 `messages` 最前，经既有 `POST /api/v1/chat/stream` 到供应商适配器（后端 `ChatMessageIn.role` 已支持 system，未新增请求字段、未改后端产品代码）；重试沿用原快照、课程修改只影响新轮；课程删除/归档不改写会话与消息、不自动换绑，展示层如实标注；资源仅登记（R-11 三态），RAG 未接入，covered 为学员手判。证据 [批次证据](../qa/H1-COURSE-SESSIONS/README.md)与独立验收（[r1 需修订](../qa/H1-COURSE-SESSIONS/A1-REPORT-01.md) → [r2 **可交付**](../qa/H1-COURSE-SESSIONS/A1-REPORT-02.md)）；真实供应商**本轮未外呼、未验证凭证有效性**（not_run） |
| A-source-locate | 来源回链与消息定位 | 目标自有（对照参考会话深链）；业务页 → 聊天 | 已验收 | 2026-09-14 B-CHAT-SOURCE-FINISH：题库/笔记产物保存时落库真实 sessionId（E2E 走真实保存按钮路径证明）与可选 messageId；进入 `/chat/<sessionId>?message=<messageId>` 后**仅在该会话内**定位该消息并提示「已定位到来源消息」；消息已删提示「原消息已不存在」，会话与原内容保留；失效会话给不可用态、不自动打开最近会话、不新建会话，学习记录与主动返回保留；减少动画下不依赖平滑滚动。e2e `chat-message-locate.spec.ts` 12 项 |
| A-mode | 原真实/模拟模式切换 | 目标自有，2026-09-09 用户要求删除 | 已验收 | 2026-09-14 B-H0R-CHAT-LINKS：不可达 mock 分支（InfoPanel 模拟模型块、ComposerContextChips mock 人设胶囊、mode 常量/依赖）、`mock={false}` 残留参数、误导文案与专用 CSS（`.chat-mode-switch*`/`.chat-banner.mock`/`.chat-mode-chip`/`.chat-stale-notice`）已清除；`ChatServiceKind/Conversation.mode` 保留但仅用于读取历史与测试替身。隔离验收真实失败不回退模拟。测试替身仅 tests/fixtures，旧模拟库不读不写不清空 |
| A-request-budget | 学习问答请求的统一预算与字符账目 | 目标自有（对照后端 `apps/api/app/schemas/chat.py` 硬限制）；`features/chat/model/request-budget.ts` | 已验收（前端；真实供应商 not_run） | 2026-09-23 CHAT-CONTEXT-BUDGET v1：课程块 + 历史 + 当前问题同一预算内构建，遵守 200/32000/120000 硬限制；裁剪阶梯「课程字段 → 整条丢最旧历史 → 整体丢课程块」；当前问题逐字不裁剪、放不下时发送前明确提示且保留输入（store 预检不产生占位/sending）；`ChatMessage.requestBudget` 随助手消息持久化（字符估算，刷新可核）；课程块字段限幅 name≤80/nextTitle≤120/conventions≤1200/资源≤12×80/整体≤min(2400,31999) 且「仅登记、未解析未检索」免责句不被砍尾；旧超长快照同样安全构建、不清库。冻结探针反转：原探针 2/2 按预期失败（D1 32088→208 字；D2 2040→1990≤2000），产品路径反转探针 3/3 通过；全量 e2e 195/0/0。**数字均为字符估算，不宣称精确 token**；真实供应商调用未外呼（not_run）。证据 [批次证据](../qa/CHAT-CONTEXT-BUDGET/README.md) |
| A-rag-mode | 学习问答「RAG 模式」入口 | 目标自有（对照后端 `/capabilities` 的 `rag: planned`） | 真实服务未接入（入口可见、不可选） | 2026-09-23 UX-PERF-CLOSEOUT v1：在原「更多能力」位置新增与其余模式同级的一行「RAG 模式」，同一行样式与同一 `aria-pressed` 选中语义，**无二级菜单/选择弹窗**；因真实 RAG 未接入而 **disabled**，原因以行内徽标「未接入 · 规划中」直接可见（3 视口实测未被裁切），与后端 `feature="rag", status=planned` 一致；`ChatWorkspace.submit()` 另保留专门阻断文案作纵深防御——**不发请求、不返回模拟检索结果、不发到普通聊天冒充**。 |
| A-rag-adapter-contract | 内部 RAG adapter 契约（未接入） | 目标自有（对照 `F:\ZQKY_RAG` 冻结 P8-FREEZE-20260922-190500 的输入输出与引用坐标） | 真实服务未接入（契约/准备已完成） | 2026-09-23 RAG-I0-PREP v1：`apps/api/app/contracts/rag_adapter.py` 定义 `RagQuery/ScopeRef/EvidenceItem/Citation/RagAnswer` + `RagAdapter` 端口 + 状态枚举（非 ok 不夹带载荷）+ 半开字符区间/1 基闭行号 + Python 码点 ↔ 前端 UTF-16 双向转换；`get_rag_adapter()` 恒定抛 `RagAdapterUnavailable`（无假实现、无路由、capability 仍 planned）；35 例合成数据契约测试。**RAG 未接入、未启动模型、未跑上游评测，质量 not_run**；上游缺口 G1–G12 见 [报告](../qa/RAG-I0-PREP/README.md) |
| A-model | 模型选择 | 指定主页模型弹层 + 原 ChatComposer 模型选择器 | 已验收 | 2026-09-09：输入区向上弹层、实际模型品牌图标、搜索/连接分组、默认/会话级选择、管理跳转、Escape/选择焦点与生成禁用；三个视口验证；仅指当时聊天选择器，不涵盖新模型管理/供应商任务 |
| A-model-management | 连接/模型管理重做 | 固定DeepTutor ConnectionsEditor/ServiceConfigEditor/ModelCards/ModelListPicker | 已验收 | 2026-09-13 MODEL-EXEC v3：供应商卡片→详情弹窗→模型发现多选→参数/推理表单，保留蓝色主题；打开编辑与设默认分离、凭证独立清除（R-08）、附加请求头编辑、创建前先收集必填草稿。首轮候选 1805397 经独立审查 needs_revision（MR-07/08/12/13/14/16 为前端项），**本批已逐项修复**：保存后用服务端值重建草稿、切连接草稿隔离、关闭先确认、导入失败保留勾选并显示错误、恢复附加头、无默认地址先收集 Base URL。回归：单测 11 项 + e2e 8 项 + 真实联调 5 项。后续4ef803b/9965592独立复验已通过已测功能范围；嵌套、草稿与焦点有正式e2e。视觉为实施队长检查，动画partial；不等于全设置或全部真实供应商通过 |
| A-provider-parity | 参考已有LLM供应商 | 固定DeepTutor services/provider_registry.py、llm/provider_factory.py、provider_core/* | 实现待验收 | 2026-09-13 MODEL-EXEC v3：`registry.py` 实装38条（36现行+2 legacy），6 backend 分派；专用 Azure/Codex/Copilot/CodeBuddy；发现来源；受控推理；v1→v2 迁移。独立审查 needs_revision 复现 MR-01/02/03/04/05/06/09/10/11/15 并**全部修复**：Responses 分派、本机免 Key 可调用、受管键可持久化、并发/删除/备份一致、Codex 取消-过期-续期、Copilot 退出。回归 `tests/test_review_regressions.py` 17 项；后端 181/181。后续独立验收已确认合同/隔离行为；真实仅DeepSeek指定场景通过，其余not_run（缺凭证/本机服务），逐条见 `_work/model-providers-v1/provider-coverage-38-implemented.md` |
| A-shell-nav | 全站主页/公共导航/错误页壳 | WorkspaceShell + navigation 登记表 + 固定DeepTutor SidebarNav 当前项语义 | 已验收 | 2026-09-13 B-H0R-SHELL：单一主页来源 HOME_PATH=/chat（品牌按钮/404/规划页/默认标题派生）；单一解析器 resolveCurrentNavigationId（桌面标可见父菜单、抽屉标隐藏项，有对应菜单的路由各1个当前项，未知404不伪造当前项）；StatusShell 使 404/错误页恰好一层壳。独立验收者亲自浏览器遍历 14 路由+404+抽屉并给命中表（e2e `shell-home-nav.spec.ts` 27 项）；错误页reset运行时未验 |
| A-ask-user | ask_user 追问卡 | AskUserOptions.tsx、use-card-submission.ts、ChatStateAdapter submitUserReply | 真实服务未接入 | 追问组件、同轮状态机与历史渲染保留；当前真实SSE不提供wait-user，不能新发起此闭环。旧模拟测试仅历史；接入真实事件后补全卡片状态、提交归属与取消恢复验收。 |
| A-tools | 工具过程 | trace/TracePresentation | 真实服务未接入 | 工具过程渲染、callId身份/状态守卫与历史展示保留，生产无工具执行事件；视觉与真实通道待验收。 |
| A-extensions | 扩展选择与目录联动 | ChatComposer 上下文选择 | 真实服务未接入 | extension-catalog 与设置唯一管理保留；主聊天显示“扩展·真实模式尚未接入”，选择/快照组件存量不等于可执行扩展。 |
| A-capability | 业务能力选择与配置门控 | ChatComposer 能力菜单、CapabilityConfigCard | 真实服务未接入 | 目录/配置/快照和门控代码保留，但主聊天除普通对话外均禁用；不恢复旧模拟生产服务。完整配置状态随真实执行通道接入验收。**2026-09-23 UX-PERF-CLOSEOUT v1：菜单改为单层**（`对话 / 追问澄清 / 智能出题 / 可视化 / RAG 模式`），原「更多能力」飞出层与三项专属能力整体移除，面板横向滚动缺陷同时修复（1440/1024 实测 `clientWidth = scrollWidth`、`scrollLeft` 恒 0）。|
| A-deep-solve | 深度解题 | deep_solve 相关 | 已移除 | 2026-09-23 UX-PERF-CLOSEOUT v1：该模式只存在于「更多能力」二级飞出层且真实模式下不可选（不可达产品代码），已随入口整体删除目录项、图标、专属配置与测试替身分支；历史会话消息里的能力快照字段保持可读、不迁移不清库。真实通道接入时按保留的四类模式设计，不恢复此值。 |
| A-deep-question | 深度出题 | deep_question 相关 | 真实服务未接入 | 旧模拟ideation/generation及题库产物测试为历史；当前无主聊天执行入口，题目保存/来源联动需随真实通道核验。 |
| A-deep-research | 深度研究 | deep_research 相关 | 已移除 | 2026-09-23 UX-PERF-CLOSEOUT v1：同 A-deep-solve，整体删除目录项、研究配置表单、演示生成器与测试替身分支；`ReportArtifactData` 渲染类型保留（旧会话已保存的 report 产物仍可展示）。原「两阶段追问 → 同轮续答 → 产物」覆盖改用保留的「可视化」能力在 e2e/单测中继续回归。 |
| A-visualize | 可视化 | visualize 相关 | 真实服务未接入 | SVG/HTML/Chart/Mermaid产物组件存量保留；主聊天生成禁用，真实配置/事件/错误恢复待接。 |
| A-math-anim | 数学动画（Manim 路由） | visualize math_animator | 真实服务未接入 | 历史模拟产物可由组件呈现，Manim真实执行未接；不能从当前主聊天发起。 |
| A-mastery | 精通之路问答 | MasteryQuestionCard/HandoffCard、ask-user-state | 待实现 | 阶段交接/新轮机制（区别普通 ask_user）（S4） |
| A-artifact | 产物与结果工作区 | SessionViewerPanel、SessionActivityPanel | 实现待验收 | 复合身份、多标签/宽度和多类型产物渲染、保存操作组件保留；普通真实SSE没有artifact事件，历史展示与新产物生成分别验收；来源与跨页联动待核验。 |
| A-message-ops | 消息操作 | ChatMessageList | 实现待验收 | 复制、引用、消息菜单与结果工作区切片已有；完整对照验收和全业务联动待补，复用统一投影。 |
| A-persona | 角色/persona | ChatComposer persona | 实现待验收 | /space/personas 与聊天共用目录，选择与会话归属已有回归；后续跨业务输入区仍需共享同源目录。 |
| A-attachments | 附件/文件 | ComposerInput | 部分实现 | 选取/拖入/粘贴/配额及异步身份代码已有；主聊天拒绝实际附件发送并保留编辑内容，真实解析未接。R-03已删除指向不存在模拟模式的提示，真实附件服务仍未接，不恢复模拟服务。 |
| A-voice | 语音输入 | ChatComposer 录音入口 | 实现待验收 | 无 STT 服务：明确“未接入”说明+带标识演示转写，不采集音频；真实权限/设备拒绝状态待真实服务接入；e2e chat-composer.spec.ts |
| A-reading-companion | 沉浸阅读伴生 AI | reading/workspace/ReadingCompanion、ReadingComposer | 部分实现 | companion-service 已使用统一 ChatService 事件模型，已有显式模拟流式/取消/草稿归属；并非旧同步模板。**R-09 已修（2026-09-15）**：滚动跟随由用户控制（上滚不被强拉回、"回到最新"显式触发且只滚伴生容器）、已测轮次按会话归属、会话切换有前进/后退历史。**READ-RETRY/READ-END 已修（2026-09-18，a9ebcaa）**：错误态重试放行（受控首败后修复）、finalizeTurn 按 turnId 幂等（重复/迟到 end 不重复落库），组件级替身 5 场景回归；显式模拟性质不变。**R-09 滚动跟随缺陷已修（2026-09-19，`a9c28ea`）**：受控诊断确证流式拉底产生的 scroll 会经 `dist<90` 重置 `followBottom`、导致用户上滚被吞（现场探针抓到 top=0→179→212 序列）；修复用同步意图 ref + 程序化滚动标记，**保护区 19 条机制语义未变**，压测 10/10（修复前 3/10 失败）。**B-R05-EXTEND v5 视觉**：按钮过渡、tab 截断与 busy、错误横幅可关闭（只隐藏视觉、重试仍可用）。过程/追问/产物/来源完整性、媒体视图仍需补验，见 [STATUS问题与批次索引](../STATUS.md#3-问题台账与验收限制)。 |
| A-kb-ingest | 知识库导入→解析→索引 | 参考 `lib/knowledge-helpers.ts`（resolveKbStatus/kbHasLiveProgress/IndexVersion）、`components/knowledge/KbStatusBadge`、`KbIndexVersionsSection` | 显式模拟（前端闭环已验收） | 2026-09-11 B-H1-KB：`services/knowledge-ingest.ts` 复刻 registered→parsing→indexing→ready、进度、取消、失败重试、刷新恢复、全部就绪追加索引版本；产物为本地结构化样例并全程标注。真实文件解析/向量检索未接入，与真实服务验收分开记录；参考另有 WS/SSE 进度与 KB 级状态（target 以逐文档状态 + KB 汇总呈现），接入真实服务时按参考协议重对齐。e2e `knowledge-notebooks.spec.ts`。**B-R05-EXTEND v1（2026-09-18）**：状态徽标带图标（处理中 Clock3 / 就绪 Check / 失败 AlertTriangle）、版本行状态章、批量与重试按钮进行中禁用防重入（A1 实测 `disabled` 生效）；显式模拟性质与标注未变 |
| A-nb-records | 笔记本记录交互 | 参考 `NotebookConsole`/`NotebookRecordRow`（记录行、展开、类型徽章） | 实现待验收 | 2026-09-18 B-R05-EXTEND v1：railed 选中指示条与描述行、四类多色类型徽章、展开区 pop-in、ConsoleNotice 空/错态、深链错误独占、删除后 URL 规范化、`popstate` 同步选中；三个操作钮与 aria-label 按队长裁定保留（未合并为菜单）。记录编辑器形态与 Markdown 渲染未纳入本批；A1 复验 pass 14/14。数据仍为本地仓储、来源回链语义未变 |
| A-books-pipeline | 书籍提案→大纲→模拟编译（含暂停/中断/失败重试） | 参考 `BookCreator`/`BooksRoute`/`BookGenerationActivity`/`BookPausedBanner`/`BookSidebar` 与后端 `book/engine.py`；目标以本地确定性模拟执行器实现 | 部分实现 | **2026-09-20 H1-BOOKS-PIPELINE v2（本批）**：七态状态机（draft/spine_ready/compiling/paused/ready/error/archived）+ 页面六态/块四态；「确认大纲」不再是同步 ready——建骨架写 run 检查点后由 `services/book-generation.ts` 的**本地模拟执行器**逐章逐块推进（增量事件、每书租约单执行者、检查点续跑、AbortSignal）；活动条 46px（阶段两段裁剪 + `n/m 章` + `mm:ss` + 展开 `role=dialog` 详情）、暂停横幅（user/provider 分叉，provider 写明"模拟供应商连续失败"）、块失败卡/页失败面板/整页重生成/整轮失败「重试生成」、归档只读、导出未完成如实标注、作答版本关系由 `contentVersion` 真实写入。**全部为本地模拟**：不接真实 LLM/解析，注入场景（块/页/整轮存储失败、供应商连续失败暂停）均为本地显式标注。独立验收 A1 **pass 32/0/0** 见 [报告](../qa/H1-BOOKS-PIPELINE/A1-REPORT.md)；BookChatPanel、课程学习会话、真实 HealthBanner 数据、侧栏折叠仍属后续批。2026-09-18 B-R05-EXTEND v2 的 busy 态/加载提示/侧栏截断保持 | **2026-09-22 H1-BOOKS-HARDEN v1**：生命周期与归属加固——任一非运行出口统一收尾（定时器/心跳/pagehide/注册表/自有租约 + 取消在途修复）；读取被拒与删除分开处理且都可恢复；页/块修复返回 `Promise<RepairResult>`，启动冻结 runId、每次写入核对归属、同页互斥可取消；租约带 nonce 写后读回校验、每步/心跳校验归属、失权即停；共享集合写入加写标记冲突检测与有界重放（**不宣称强原子性**）；最终完成落库失败落 `kind storage` 失败并给「重试生成」。证据 [批次证据](../qa/H1-BOOKS-HARDEN/README.md)、[首败台账](../qa/H1-BOOKS-HARDEN/DEFECT-LEDGER.md)。仍不接真实 LLM/解析。 **2026-09-22 H1-BOOKS-COMMIT-SAFETY v1**：共享集合（书籍、作答）写入互斥——生产路径用原生 Web Locks、jsdom 回退锁；锁内完成读快照/变更/修订号/数据/双重写后校验，校验不过整事务在最新快照上重做（≤3 次）；提交结果区分六态且非 `committed` 不返回值；调用方全部迁移为等待提交结果（表单/跳转/成功提示后移，失败保留输入与草稿）。证据 [批次 README](../qa/H1-BOOKS-COMMIT-SAFETY/README.md)、[首败台账](../qa/H1-BOOKS-COMMIT-SAFETY/DEFECT-LEDGER.md) 与真实双标签页 e2e。
| A-course-syllabus | 课程大纲与资料 | 参考 `CourseSyllabus`/`CourseResources`（进度条、单元行、资料行） | 实现待验收 | 2026-09-18 B-R05-EXTEND v2：大纲加 progressbar 进度条（width 300ms）、单元位置编号/covered 删除线/下一单元高亮/topics 截断；资料行 kind 图标 + 不可用后缀独立 shrink-0 + 移除钮 hover/focus 显隐；保存/归档 busy。**R-11 三态与重试链路经 A1 独立回归**（available/missing/unknown 区分、失败重试恢复、原始损坏数据逐字节未变）；课程学习会话、聚合磁贴、Mode/Persona 按裁定属 H1 |
| A-writing-ai | 协同写作 AI 修改（选区改写/润色/扩写/全文生成） | 参考 `CoWriterWorkspace` + `useSelectionEdit`；目标以统一事件模型显式模拟 | 实现待验收 | 2026-09-19 B-R05-EXTEND v3：编辑器正文区样式迁出内联（focus 环）、保存三态 chip 色彩化、AI 面板/预览按钮过渡；**事件模型、取消/失败重试、应用前自动快照、撤销栈、版本恢复逻辑零改动**，`【模拟生成】` 标注与 `AI 改写预览`/`AI 修改预览` aria-label 逐字保留。DOCX 导入（参考 `importCoWriterDocx`）与分栏/同步滚动按裁定属后续批；A1 独立验收 pass 0 fail。显式模拟性质未变 |
| A-reading-library | 阅读库级页面（集合/材料库） | 参考 `reading/library/*`（LibraryShell、ReadingLibrary、MaterialLibrary） | 实现待验收 | 2026-09-19 B-R05-EXTEND v3：集合卡片 hover 与计数图标、材料库筛选 2→4 tab（解析中/失败，**计数来自真实数据**）、状态色点（ready/failed/busy 复用 space-pulse）、行头窄视口 wrap 修复；模拟解析四态与取消/重试/分配路径、`模拟解析` 标注与「文件名（模拟导入，不读取真实文件）」说明逐字保留。LibraryShell 双 tab 化与真实解析按裁定属后续批；A1 独立验收 pass 0 fail（含 R-09 工作区不回退实测） |
| A-settings-page | 设置页（分类导航/搜索/扩展管理） | 参考 `components/settings/SettingsNav.tsx`、`SettingsMain.tsx`、`SettingsToolbar.tsx`、`SettingsOverview.tsx` | 实现待验收 | 2026-09-19 B-R05-EXTEND v4：索引导航图标+双态+焦点环（保留 `aria-current="location"` 与 160ms 参数）、搜索框 focus+清除按钮（`清除搜索`，全仓唯一）+无结果空态、ExtensionManager 卡片/开关视觉、外观与关于分区排版。**模型区 contract-v1 未触碰**（`model-settings/**` 与 `chat/**` diff 全空；5 处可访问名称锚点原样）；参考式页级草稿工具栏/状态条/就绪面板按裁定不做（无设置草稿层）。A1 pass（含模型区 mock 成功态全链路；**mock 不等于真实供应商通过**） |
| A-lesson-plan | 教案工作台（本地规则填充/草稿/导出） | 目标自有（参考 /chat 视觉基准）；`features/lesson-plan/AGENTS.md` 约定 | 实现待验收 | 2026-09-19 B-R05-EXTEND v4：表单分区/type-chip/模板卡视觉、预览工具栏 hover/disabled、toast 与 storage-alert 视觉（文案与 role 未动）、导航分段微调、导出菜单进场 180ms。**导出与草稿链路零改动**：`print.css`/`@page lesson-plan`/Word 映射与模板校验/`DraftRepository`/600ms 防抖与 flush/`pagination.ts`/`--serif` 回退链均未触碰；A1 复核 **`verifyDocx` 全项通过**、损坏草稿不覆盖、草稿恢复与跨页返回；教案仍是本地规则填充（非 AI 生成），14 个布局断言锚点类名保留 |
| A-space-sections | /space 四子页既有交互（会话历史/题库/角色/CLI） | 参考 `components/space/*` 与 `cli-apps/*` | 实现待验收 | 2026-09-19 B-R05-EXTEND v5：四页视觉统一（卡片/列表/工具条、计数 chip、刷新 spinner、refreshing 变暗、CLI 状态徽标、行头窄视口 wrap）。**真实计数/筛选/搜索/批量/演示载入/来源回链（R-10）全部保留**；space.css 只读、增量进 space-sections.css。A1 复核 space-pages 8/8、chat-source-links 21/21、chat-message-locate 12/12。CLI 目录搜索/详情/分页（依赖远程 catalog API）与角色卡 hover 显隐按裁定不做 |

本次知识库相关既有e2e 10/10通过，1920/390的已就绪、失败、解析中和系统减少动画状态样本补验通过；仍无真实文件解析/索引/检索调用。所有结果仅更新文档，未修改生产模型配置或能力标记。

## 统一事件约束（已实施部分见 chat-service.ts）

事件联合已实施为 **type 判别式联合类型**：turn-start、text（必带 delta）、reasoning（必带 delta）、process、stage、tool（必带 call/kind/name/status，状态 running/done/error/cancelled）、wait-user（必带 interaction：interactionId/intro/questions/status）、artifact（基础消费已实现）、usage、end、error。全部事件携带 sessionId/turnId；终态（end/error/取消）后拒绝一切后续事件。
**提交归属（R11）**：submitReply 结果只归属发起时的会话/轮次/交互/提交身份（submissionId 实际用于归属判定与服务侧单次消费）；事件顺序=确认→续答/收尾事件→提交返回值。
**消息投影（R12）**：`conversationProjection()` 统一正文+已确认追问交流+逐卡续写；复制、服务请求上下文、预算计算共用。
扩展快照发送时冻结；真实服务不读取、不发送模拟字段，SSE 协议未变。
待补齐：产物完整参考验收与跨业务消费、mastery新轮机制、阅读伴生的完整参考交互。ChatService类型联合比真实HTTP事件更丰富，类型存在不代表当前供应商能产生该事件；真实ask_user、工具及复杂能力通道待接，主聊天不恢复模拟。
