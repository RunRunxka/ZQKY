# 动画清单与验收边界

更新：2026-09-24（CHAT-CONTENT-MATH-AND-FOLLOW v1 复验 M-reasoning-fold 用户重开后流式跟随/上滚让位行为；未重新采样逐帧折叠曲线，动画验收状态不升级）。UX-PERF-CLOSEOUT v1 与 UX-REGRESSION-FIX v1 历史折叠动画参数和减少动画证据保留；2026-09-20 曾新增 M-books-strip / M-books-paused。全站视觉基准为当前学习问答；模块专属动画对照固定 DeepTutor，原版没有则不添加。下表“已验收”均绑定具体日期、候选和范围；旧模拟生成动画不能据此视为当前可发起能力。历史65项、teardown首败及后续修复见 [交付历史](../archive/DELIVERY_HISTORY.md#snapshot-status-20260910)，当前计划与验证边界见 [STATUS](../STATUS.md)。

状态词汇：待实现、部分实现、实现待验收、已验收；逐项记录开始/过渡/结束、快速中断、减少动画与焦点。

2026-09-12 B-ACCEPT补验：候选99aea55现有e2e 80/80通过，其中`chat-home.spec.ts` 4项、`sidebar-transition.spec.ts` 4项、`sidebar-chat-fixes.spec.ts` 5项重新验证思考球像素变化/系统与本地减少动画、右面板退出中断/inert/拖拽Escape清理、三视口聊天与导航过渡。实际DeepSeek Flash8192样本又验证推理自动展开/收起与手动优先，但不声称全站动画或推理动画曲线已完整验收。数据与截图见 [本次结果](../qa/acceptance-20260912/summary.json)。

| id | 场景 | 参考参数与来源 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| M-composer-width | 输入框宽度 | 指定主页 composer max-width 912px，外层含 48px 留白 | 已验收 | 2026-09-09 用户参考覆盖旧 768→960 扩宽；首次发送 20 帧采样证明输入列不横向跳动；测试同步保留 textarea 150ms 高度过渡 |
| M-pop | 输入区弹出层 | 指定主页入场 180ms cubic-bezier(.16,1,.3,1)，y6/scale.96；保留项目退出 160ms y4/scale.97 | 已验收 | 2026-09-09 菜单进出场、快速开关、inert/焦点回归通过；当前录像和定格帧位于 docs/qa/chat-home-20260909/；本批范围仅学习问答 |
| M-trace | 过程展开 | TracePresentation.tsx：300ms，Tailwind ease-out=cubic-bezier(0,0,0.2,1) | 实现待验收 | --ease-standard 与弹出层缓动区分；中点帧已人工查看 |
| M-ask-card | ask_user 卡片 | AskUserOptions.tsx：卡片无进场装饰动画；选项 transition-colors 150ms | 实现待验收 | 组件保留原无装饰进场规则与150ms选项颜色；旧模拟e2e已删除，当前真实无ask_user事件，接入后重验完整运行过程。 |
| M-config-list | 模拟配置列表插入 | 160ms opacity/translateY | 部分实现 | 历史模拟配置组件有160ms插入；主聊天执行禁用，退出/重排和真实运行场景未验收。 |
| M-settings-nav | 设置分类悬停与定位 | 160ms 背景、滚动定位 | 部分实现 | 基础行为已有 |
| M-model-management | 供应商/模型卡片、详情与参数折叠 | 固定DeepTutor ModelCards.tsx：background-color/border-color/transform 150ms、active scale .995，箭头150ms位移反馈 | 实现待验收 | 2026-09-13 MODEL-EXEC v3 实装：卡片 `transition: border-color/background-color/transform 150ms`、`:active scale(0.995)`、箭头 hover translateX(2px)；`@media (prefers-reduced-motion: reduce)` 关闭卡片/箭头/选项过渡与 spin 动画。e2e `model-settings.spec.ts` 覆盖三视口与减少动画可打开详情，真实联调亦覆盖减少动画；本批新增「关闭未保存更改先确认」的行为断言（非曲线）。后续9965592有嵌套关闭/草稿与焦点返回行为回归；快速中断和曲线未完整验收，故不升为已验收 |
| M-shell-404 | 404/错误页壳与当前菜单切换 | 复用公共壳侧栏 200ms 折叠与抽屉原生 transition；无新增动画 | 实现待验收 | 2026-09-13 B-H0R-SHELL：404/错误页现有一层公共壳；减少动画下仍可用、三视口无溢出。仅静态截图与类名断言，未逐帧采样壳内切换曲线，故不升为已验收 |
| M-chat-locate | 来源消息定位与高亮 | 目标自有：`scrollIntoView({block:'center'})`，减少动画时 behavior=auto；高亮为临时描边 | 实现待验收 | 2026-09-14 B-CHAT-SOURCE-FINISH：`smooth` 仅在非减少动画时使用，reduced 下改为瞬时定位并仍给出「已定位」提示（e2e 覆盖）；未逐帧采样曲线，故不升为已验收 |
| M-reduced | 减少动画 | prefers-reduced-motion + 本地覆盖 | 已验收 | 2026-09-09 补 Canvas 本地偏好监听；浏览器像素采样验证普通状态持续变化、系统/本地 reduced 均静止；原 CSS 全局机制保留 |
| M-sidebar-panel | 侧栏/右面板宽度 | 学习问答左栏 200ms，右栏/聊天避让 220ms ease-out | 部分实现 | 2026-09-10 H0 候选：220/56px和200ms统一在公共壳；1440/1920的软导航逐帧、菜单几何与字体比较、折叠状态/历史返回回归；390抽屉遮罩/焦点/关闭/字体比较。隐藏直达页当前菜单已有e7fb2a4功能验收；其他模块专属面板与完整动画仍待验收。 |
| M-first-send | 欢迎区→首次发送 | 学习问答固定 912px 输入内容列；textarea height 150ms ease-out | 已验收 | 2026-09-09 主页候选：固定内容列、textarea过渡、同一发送/停止按钮与三视口通过；范围限学习问答。 |
| M-artifact | 产物面板展开/退出 | 指定主页 translateX(100%)↔0，220ms ease-out | 已验收 | 2026-09-09 主页候选：入场/退出中间帧、快速重开、退出 inert、拖动中 Escape 清理、标签与持久宽度回归通过；范围限学习问答历史产物 |
| M-reasoning-fold | 推理折叠展开/收起与流中跟随 | 固定 DeepTutor AssistantActivity：阶段自动展开、正文出现自动折叠、手动选择优先；折叠用 `grid-template-rows 0fr↔1fr` 300ms `cubic-bezier(0,0,0.2,1)` + opacity，200ms chevron | 已验收（历史折叠语义；本批 follow 经独立验收复验，曲线未验） | 2026-09-23 UX-PERF-CLOSEOUT v1 **未改动**折叠动画参数、既有正文阶段自动折叠语义与减少动画压制；UX-REGRESSION-FIX v1 在折叠后保留内容 320ms 覆盖 300ms 过渡。**CHAT-CONTENT-MATH-AND-FOLLOW v1（2026-09-24，独立验收 pass）**：正文开始后仍有 reasoning 增量时自动折叠保持；用户重开后内层跟随恢复（10 次采样 gap 全 0）；真实滚轮上滚后 top 恒 0 而 scrollHeight 275→423（约 2.2s、14 次采样）未被抢回，回底后 gap 全 0。独立验收另在 1440×520 短视口确认外层有溢出时推理区仍完整可见（clippedPx=0）；键盘/硬件触摸/reduced-motion 及折叠逐帧曲线未执行，曲线仍属 H6，不升级历史动画总验收。 |
| M-thinking-orb | 思考球与状态文字 | vendored Thinking-orbs：20px 预设/18px 显示/3x；working/solving speed 1，breathing speed .5；文字 1.8s opacity .45↔1 | 已验收 | 2026-09-09 主页候选：复制参考算法，蓝色 currentColor；Canvas 实际像素变化与静止验证、离屏/隐藏暂停源码核对；状态跟随原消息，不重放整条文本动画 |
| M-buttons | 按钮悬停/按下/禁用/加载 | 逐组件核对原版 | 待实现 | S7 |
| M-ask-states | 追问卡状态切换（预览→可答→提交→摘要） | AskUserOptions 实际过渡 | 待实现 | 原版无装饰进场；状态切换动画待逐项核对（S7） |
| M-reading-layout | 阅读导航/伴生面板/选区浮条 | ReadingWorkspace/ReadingCompanion 原版逐组件参数 | 部分实现 | R31布局/拖拽和R32手机面板有历史实现记录；本次未跑阅读专属动画验收，仍需按参考逐个核参数/中断/退出，不能将原视图缺项视为允许缩减。 |
| M-kb-progress | 知识库文档处理进度条/徽标切换 | 参考 KbStatusBadge/进度条：`transition-all duration-300`、`animate-spin`；徽标/banner 无进出场动画 | 实现待验收 | 2026-09-12补查1920/390×正常/系统reduce共4个解析中样本，35%与取消入口可读，均能到就绪；系统reduce使公共壳transition 0.2s→1e-05s。采样未观察到活动CSS/Web Animations，未证明进度内条300ms曲线或完整中断，故不升为已验收；[探针](../qa/acceptance-20260912/visual-probes.json) |
| M-space-tile | /space 磁贴悬停与计数骨架 | 参考 SpaceDashboard DashboardCard：`transition-all duration-150`、`hover:-translate-y-0.5` + hover 阴影、`animate-pulse` 计数骨架；ArrowUpRight 仅 transition-colors | 实现待验收 | 2026-09-18 B-R05-SPACE-VISUAL v1 实装：磁贴 `transform/border-color/box-shadow 150ms` + hover translateY(-2px)/--shadow-card、骨架 space-pulse 1.4s（对照 animate-pulse）、箭头 transition-colors 150ms；无参考外新增动画；reduce 下截图无异常（[qa README](../qa/space-r05-20260918/README.md)）。未逐帧采样曲线/中断，不升为已验收 |
| M-kb-tabs | 知识库页签/分区导航与卡片悬停 | 参考 KnowledgeHome.tsx:170/278（`transition-colors` + `focus-visible:ring-2`）、KnowledgeHome.tsx:193（`opacity-0 transition-opacity group-hover:opacity-60`）、KnowledgeBaseDetail.tsx:263-267（下划线页签 hover 变色） | 实现待验收 | 2026-09-18 B-R05-EXTEND v1 实装（`knowledge.css`）：页签/卡片 hover 变色与焦点环 150ms、卡片 ChevronRight `opacity 0→.6`、状态圆点脉冲 `kb-pulse` 1.4s（对照 `animate-pulse`）；无参考外新增动画；reduce 下 A1 实测压至 0.01ms。未逐帧采样曲线/中断，不升为已验收 |
| M-kb-actions | 知识库进行中态与版本行 | 参考 KnowledgeBaseDetail.tsx:235-249（`disabled={retrySubmitting…}` + Loader2 `animate-spin`）、KbIndexVersionsSection.tsx:262-266/525-526（按钮与版本行 spin）、:368-373（进度条 `transition-all duration-300`） | 实现待验收 | 2026-09-18 B-R05-EXTEND v1 实装：批量/重试/重建按钮进行中 `disabled` 防重入 + Loader2 旋转（复用 `.space-spin`）、文档与版本徽标进行中图标旋转、进度条 300ms 保留；A1 实测空/错/进行中态可用，reduce 压制生效。快速中断与曲线未逐帧验收 |
| M-nb-indicator | 笔记本左栏指示条与行 hover | 参考 NotebookConsole.tsx:373-377（`transition-[background-color,color] duration-150`）、:383-386（`w-[2.5px] … transition-all duration-200`） | 实现待验收 | 2026-09-18 B-R05-EXTEND v1 实装（`notebooks.css`）：激活项 2.5px 指示条 height/opacity 200ms cubic-bezier(.22,1,.36,1)、rail 项与记录行 hover/焦点 150ms；A1 实测三视口与 reduce。未逐帧采样中断 |
| M-nb-pop | 笔记本记录展开与操作钮反馈 | 参考 NotebookRecordRow.tsx:270（`animate-pop-in`）、:236-239（箭头 `transition-transform duration-200`）、:308-317（`active:scale-[0.98]`）、NotebookConsole.tsx:688（`active:scale-[0.97] duration-150`）；参考 globals.css:674-686 `dt-pop-in 200ms cubic-bezier(0.22,1,0.36,1)` | 实现待验收 | 2026-09-18 B-R05-EXTEND v1 实装：展开区 `.nb-pop-in`（同参数 from opacity 0/translateY(6px)/scale(.985)）、箭头旋转对齐 200ms、操作钮 150ms + active scale(0.97)；A1 实测展开态 `nb-pop-in` 生效、reduce 压制。未逐帧采样曲线 |
| M-nb-mobile-wrap | 笔记本窄视口换行 | 目标自有（A1 首败修复；参考在窄视口的等价行为是行内截断，本批按可用性改为换行）[推断] | 实现待验收 | 2026-09-18 `19b514c`：≤760px 行头 `flex-wrap:wrap` + 时间戳 `flex-basis:100%/order:10` 换第二行、`space-bank-layout align-items:stretch`。A1 v2 复验 pass（390 全页溢出 0，修前 56）。属修复项，非参考动画来源 |
| M-books-card | 书籍卡片悬浮与搜索行 | 参考 BookLibrary.tsx:229（`transition-all hover:-translate-y-0.5 hover:shadow-md`）、:132-143（搜索常驻 ≥640px） | 实现待验收 | 2026-09-18 B-R05-EXTEND v2 实装（`books.css`）：卡片 `transform/border-color/box-shadow 150ms` + hover 抬升 -2px + `--shadow-card`、尾部箭头 hover 位移 + 变色；A1 实测 hover matrix(1,0,0,1,0,-2) 生效、reduce 压制 1e-05s。未逐帧采样曲线/中断 |
| M-books-actions | 书籍 busy 态与两击确认 | 参考 BookLibrary.tsx:258-286（卡片内两击删除）、BookCreator.tsx:810-816（busy `disabled:opacity-*` + Loader2 spin） | 实现待验收 | 2026-09-18 B-R05-EXTEND v2 实装：创建/确认提案/确认大纲/导出加 `disabled` + `Loader2` + `.space-spin`（350ms 呈现窗口）、删除改两击确认、模拟编译加载提示。A1 实测各 busy 路径命中、两击三态正常；未逐帧采样。**2026-09-20 H1 v2 补充**：新增暂停/继续/重试/整页重生成的 busy 态（`正在暂停…`/`正在继续…`/`正在重试…`/`正在重新生成…`）与列表卡片 `compiling` 脉冲圆点（对照参考 `animate-pulse`） |
| M-books-strip | 生成活动条、展开浮层与状态点 | 参考 BookGenerationActivity.tsx:32-33（`STRIP_HEIGHT = 46`）、:234-241（`dt-detail-in` + `role="dialog"`）、globals.css:711-724（`dt-detail-in 180ms cubic-bezier(0.16,1,0.3,1)`）、:691-702（`dt-breathing 1.8s` `opacity .45↔1`）、:1104-1106（reduce 冻结） | 实现待验收 | **2026-09-20 H1-BOOKS-PIPELINE v2 实装**（`features/books/styles/book-pipeline.css`）：固定 46px 单行活动条；浮层进场实测 `book-pipeline-detail-in` **0.18s cubic-bezier(0.16,1,0.3,1)**（= 参考 180ms 同曲线）、未 settled 阶段文案呼吸实测 `book-pipeline-breathing` **1.8s**；无参考外新增进出场。实测：快速开关 0→1→0（连点 4 次仍 0、无重复浮层、`aria-expanded=false`）、展开中暂停后浮层仍可收起（计数 0）、键盘焦点环 2px solid `rgb(37,99,235)`、`prefers-reduced-motion: reduce` 下两项 `animation-name: none` 且时长 `1e-05s`（全局层压制）。浮层结构对照参考 `BookGenerationActivity` 的 `relative` 外壳 + `absolute inset-x-0 top-full z-40` 定位层（**曾因浮层挂在 46px `overflow:hidden` 行内部而被裁成一条线，r3 修复并加几何断言**，见台账 N10）；证据 [visual/evidence.json](../qa/H1-BOOKS-PIPELINE/visual/evidence.json)。未逐帧采样曲线/中断帧，故不升为已验收 |
| M-books-paused | 暂停横幅出现与恢复、整轮失败重试 | 参考 BookPausedBanner.tsx:37-68（标题 + 按 `pause_kind` 分叉说明 + 描边恢复按钮）、BookGenerationActivity.tsx:203-223（interrupted 用 `RefreshCcw` + `Continue generating`，paused 用 `Play` + `Resume generation`） | 实现待验收 | **2026-09-20 H1-BOOKS-PIPELINE v2 实装**（`book-pipeline.css` / `book-reader-states.css`）：暂停横幅（`provider` 类写明"模拟供应商连续失败"）、活动条 paused=「恢复生成」/interrupted=「继续生成」两态与 busy 文案；整轮失败横幅含「重试生成」busy 态。实测：暂停出现→活动条同显「生成已暂停」→刷新后不自动续跑且横幅仍在→点「恢复生成」真的回到“正在逐章编译”并跑完（修复 N3 的链路）；中断态点「继续生成」后完成。无参考外新增动画；未逐帧采样 |
| M-courses-card | 课程卡片悬浮与大纲进度条 | 参考 CoursesShelf.tsx:143/154-157（卡片 hover 抬升 + 箭头位移）、CourseSyllabus.tsx:142-155（`transition-[width] duration-300`）、CourseResources.tsx:228（移除钮 `transition-opacity`） | 实现待验收 | 2026-09-18 B-R05-EXTEND v2 实装（`courses.css`）：卡片 hover 150ms 抬升/阴影/箭头位移、大纲进度条 width 300ms、资料行 hover 浅底 150ms、移除钮 opacity hover/focus 显隐（≤760px 常显以保触屏可达，超出参考的可用性处理）；A1 实测进度条 50→100 联动、reduce 压制 1e-05s。未逐帧采样曲线 |
| M-writing-ui | 写作列表/编辑器过渡 | 参考 co-writer/page.tsx:262（卡片 `transition-colors hover:border`）、CoWriterWorkspace.tsx:1788（主按钮 `transition-[opacity,transform] duration-150 hover:opacity-90 active:scale-[0.97]`） | 实现待验收 | 2026-09-19 B-R05-EXTEND v3 实装（`writing.css`）：列表卡片 150ms（border/background/box-shadow）+ hover 与焦点环、按钮 opacity/scale 150ms、保存三态 chip 色彩过渡、正文区 focus 环、版本行 hover。仅 hover/focus/active 过渡，**无进出场动画**（E3 核出四页未引用 dt-pop-in）；A1 实测 reduce 压制 1e-05s。未逐帧采样 |
| M-reading-lib | 阅读库/材料库过渡与状态点 | 参考 LibraryShell.tsx（标题/tab 过渡）、MaterialLibrary.tsx:296（筛选 chip `transition`）、ReadingLibrary.tsx:307（集合行 `transition`） | 实现待验收 | 2026-09-19 B-R05-EXTEND v3 实装（`reading-library.css`）：集合卡片 hover 150ms 抬升/阴影/焦点环、材料行 hover 与操作钮过渡 150ms、状态色点（ready 静态/failed 红/busy 复用 space-pulse 1.4s 呼吸）、筛选 tab 过渡。仅过渡类动画，无进出场；A1 实测三视口与 reduce。未逐帧采样曲线 |
| M-settings-page | 设置页导航/搜索/开关过渡 | 参考 `SettingsNav.tsx:395,460`（Tailwind `transition-colors` 默认 150ms）；既有 `M-settings-nav` 记 160ms 且当前实现 `settings.css:27` 即 160ms | 实现待验收 | 2026-09-19 B-R05-EXTEND v4 实装（`settings-extend.css`）：导航项 hover/current 背景与字色 160ms（**沿用既有参数，未改**）、搜索框与清除按钮 160ms、ExtensionManager 卡片 160ms + 按钮 150ms + `active scale(0.98)`、Toggle 轨道过渡。A1 实测 reduce 下压至 1e-05s。与 `M-settings-nav` 参数一致；未逐帧采样 |
| M-lesson-visual | 教案页过渡与弹层进场 | 参考 /chat 基准 `chat-home.css:233` `chat-home-popup 180ms cubic-bezier(0.16,1,0.3,1)`（教案页无 DeepTutor 参考模块） | 实现待验收 | 2026-09-19 B-R05-EXTEND v4 实装（`lesson-visual.css`）：导出菜单进场 `lesson-menu-in 180ms` 同缓动、toast 进场 `lesson-toast-in 180ms`、type-chip/模板卡/预览工具栏按钮 hover 150ms、`storage-alert` 视觉。**仅进场、无离场**（组件条件卸载结构，按裁定不做）；A1 实测 reduce 压至 1e-05s/0s。未逐帧采样曲线 |
| M-reading-ws | 阅读工作区过渡与 tab 状态 | 参考 ReadingCompanion.tsx:331,351,370,383（`transition-[background-color,color,transform] duration-150 active:scale-90`）、参考 ReadingWorkspace.tsx:390-397（tab busy spin + `max-w-[168px] truncate`） | 实现待验收 | 2026-09-19 B-R05-EXTEND v5 实装（`reading-ws.css`，`.reading-ws-page` 作用域）：按钮 150ms + `active scale(0.97)`、材料 tab 长标题截断（桌面 168px / 窄视口 120px + ellipsis）、解析中 tab 复用 `.space-spin` 旋转、错误横幅行布局与关闭钮 hover。**仅过渡/反馈类，无进出场动画**（E5 核出参考阅读页未引用进出场关键帧）；reduce 走全局层。A1 实测 390 无溢出、ellipsis 生效、tab 可点 |
| M-space-sections | /space 四子页过渡与刷新态 | 参考 space/子页组件（卡片/行 hover、`transition-opacity + opacity-60` 刷新变暗，见 E5 附录） | 实现待验收 | 2026-09-19 B-R05-EXTEND v5 实装（`space-sections.css`，`.space-sections-page` 作用域）：卡片/列表 hover 与 `:focus-visible` 150ms、计数 chip、刷新按钮 spinner（复用 `.space-spin`）、题库 refreshing `transition-opacity 150ms + opacity .6`、行头窄视口 wrap。无进出场动画；A1 实测四页 390 零溢出、8/8 e2e 通过 |

动画证据边界：历史截图/录像和首败不删；已验收只覆盖表中日期与候选。H1–H5 随功能批同步验收模块动画，H6 再集中补漏；不以存在 animationName 或截图文件作为整段已播放证据。

R-05 在登记的既有页面推广范围内已收口（沿用2026-09-19记录，范围见 [STATUS 当前批次](../STATUS.md#current-task)）。六批（B-R05-SPACE-VISUAL v1 + B-R05-EXTEND v1~v5）的动画条目见 M-space-tile、M-kb-tabs/M-kb-actions/M-nb-indicator/M-nb-pop/M-nb-mobile-wrap、M-books-card/M-books-actions/M-courses-card、M-writing-ui/M-reading-lib、M-settings-page/M-lesson-visual、M-reading-ws/M-space-sections。**H1-BOOKS-PIPELINE v2（2026-09-20，业务闭环批）新增 `M-books-strip` 与 `M-books-paused` 两条**（生成活动条/展开浮层/暂停与中断恢复；量化参数与实测见该两行），本批未新增参考外动画。**动画精度（逐帧曲线/中断/退出）仍属 H6 总验收范围**，表中条目多为「实现待验收」不因 R-05 收口而升级。**H1-BOOKS-HARDEN v1（2026-09-22，缺陷修复批）未新增参考外动画**：读取失败面板与修复失败提示为静态 `role=alert`（无进出场），忙态沿用既有 `space-spin`，活动条/暂停横幅参数未改。`M-settings-nav`(160ms) 与 `M-model-management`(150ms/scale .995) 参数未改（模型区按裁定只读）。阅读 R-09 上滚、显式回到底部与手机 reduce 行为已测；**R-09 滚动跟随的 flaky 已定位为产品层 followBottom 竞争并修复（2026-09-19 `a9c28ea`，压测 10/10）**，详见 [STATUS §3](../STATUS.md)。
