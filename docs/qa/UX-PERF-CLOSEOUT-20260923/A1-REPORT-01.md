# 独立验收 A1 报告（第 1 轮）

任务：UX-PERF-CLOSEOUT v1 / A1（只读复验，未修改任何文件、未提交）
候选：复验期间候选发生移动，验收者按两个时点分别冻结——时点①`main@3dcace8` + 未提交工作树（FROZEN r2）；
时点②`main@d65c9ca`（FROZEN r3）。构建：`.next = ZkSw3NYEUtNUBATU0sde5`、`.next-test = 0ZQwdSFXSsgSdMsFPOlSB`。
**判定：needs_revision**（1 项必修 + 1 项条款未实现 + 6 项口径/记录问题）

## 逐项结论（摘要）

| # | 项 | 判定 | 关键观测 |
| --- | --- | --- | --- |
| 1 | 指纹与范围 | pass（附口径注） | r2 工作树字节 43/43 全等；r3 git blob @61a9b12 = 49/49、@HEAD = 47/49；越界文件 0、`.env*` 未触碰、无密钥命中 |
| 2 | 50k 性能复现 | pass | 独立复跑：帧 p95 **5.70 ms**、max 16.70 ms、>50 ms **0**、脚本 **1098 ms**、layout 2034 ms、DOM **385**、IDB 65 次/1.81 MB、首个可见增量 **48 ms**、2000 块/31.0 s |
| 3 | 正确性回归 | **fail** | `18/19`，唯一失败项为 README 声称 PASS 的「用户上滚不被拉回」，观测 `0 → 4052` |
| 4 | 布局变差是否可接受 | pass；但「唯一变差项」表述 fail | 总主线程 task 31 215→5 837 ms、长任务 210 条/23.1 s→0 |
| 5 | 菜单（3 视口） | pass | 三视口 `client=scroll=278`、不可横滚、无 `.chat-cap-more/-flyout`、Escape 焦点回触发器；RAG 行 disabled、徽标「未接入 · 规划中」未裁切 |
| 6 | 旧模式回流/后端 | pass | 源码命中只剩注释与「不得回流」断言；**`apps/api` 差异 0 字节**、Python 命中 0 |
| 7 | RAG 可否被选中 | pass | 原生点击超时、force 点击/键盘均无法选中，触发器恒为「当前：对话」 |
| 8 | 导航/学习记录/favicon | pass | nav 220、`.chat-page` 仅 `chat-main`（无 236px 列）、18 项会话在 `.global-nav` 内且可滚、底部导航可见；手机单弹窗、Tab/Escape 正确；`/books` 等唯一高亮教材资料库；`/icon.svg` 200 312B 稳定 |
| 9 | 行为回归 e2e | pass | 6 spec **47 passed**；`space-pages` 8 passed；搜索/重命名/归档/删除/深链/课程往返均覆盖 |
| 10 | 字体 | pass | 18 组合 × 2 模式页面溢出全 0；导航/聊天为衬线链、其余为黑体链；阻断后 0 字体请求且 `fonts.check(Chat Lora)=false` |
| 11 | 硬编码字体名 | pass | 具体字体名只剩 `globals.css` 两处 `@font-face` |
| 12 | 教案工作台 | pass | 面包屑隐藏、h1 与 /co-writer 一致、DOM 顺序 editor→config→preview、唯一折叠按钮、4 次连点同节点、打印下 `.paper` 可见、**打印下 `/chat .chat-page` 高 900（非 0）** |
| 13 | 文档事实性 | **fail**（数字对、表述不准） | §3.1 与原始 JSON 逐项全等；unit 440、api 217 与 §5.A:10 一致；但发现 3 处口径问题 |
| 14 | not_run 口径 | pass | 真实供应商/RAG/硬件触摸/逐帧动画均如实 not_run |
| 15 | R-14 | pass（定性成立） | 隔离 `--repeat-each=3` 3/3 通过；全文件顺序 6 passed；**失败日志首败行为第 266 行**（活动条超时），而 250–256 行的「两本书笔记均已在存储中」poll 已通过 → 日志本身证明无数据丢失 |

## 检出的问题（全部已处置，见 A1-REPORT-02）

1. **必修**：「用户上滚不被拉回」是**本批引入的行为差异**（rAF 内未复检 `following`），非探针问题；
   验收者用独立探针逐 100 ms 采样复现「首次上滚被吃掉（置 0 后首个采样点已 2095）→ tail 4052」，
   并指出机制：`scrollTop = scrollHeight` 自身触发的 `onScroll` 会把 `following` 重新置 true。
2. **T3 条款未实现**：历史旧能力值只有数据保留（字段在、无迁移/回填/清库），**没有任何渲染路径**展示
   `extensions.capability`；审计文档以「不会被错标」替代了「明确降级展示」。
3. **口径订正 3 处**：① 长任务数据其实有效（210 条/23 078 ms → 0），文档却写成「取不到」，且与
   同文「合入依据是帧间隔与长任务」自相矛盾；② 「唯一变差项是布局总量」不成立（跟随行为也变了）；
   ③ JS 堆 11.4 MB 不可复现（复测 18.8～24.3 MB），「峰值」措辞错误。
4. **记录/工具 3 处**：冻结记录的哈希口径未写死（工作树 CRLF vs blob LF 会得到 34/49「不符」的假告警）、
   headCommit 与 tip 不一致、记录文件自引用必然过时；探针缺「上游来源自检」（rewrites 在 build 时固化，
   用 `ZQKY_API_ORIGIN` 起服不改写代理目标，曾产生「请求 50000 字但页面 20000 字」的静默错配）；
   内容溢出条目名与实际不符。
5. **流程问题（我方）**：验收期间我并发跑了 trace 采集并重启了共享上游，污染了验收者一轮的 `upstream.*` 字段。

## 验收者未执行 / 无法验证

真实供应商外呼（无凭证）、真实 RAG、移动端硬件触摸、逐帧动画曲线均为 not_run；
全量 200 例 e2e 未由其独立重跑（其核对原始日志与环境可比性）；typecheck/lint/build 未重跑；
字形像素级回退未验（仅验到阻断后 0 请求 + `fonts.check=false` + 页面溢出 0）。验收者声明本次不做视觉验收。

证据目录：`_work/verify-A1/`（只读产物，Git 忽略）。
